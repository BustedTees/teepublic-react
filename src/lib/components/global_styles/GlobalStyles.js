import React, { Component } from 'react';

import './GlobalStyles.scss';

export default class GlobalStyles extends Component {
  render() {
    return <p>This is a hack to bundle global styles in the build process.</p>;
  }
}
